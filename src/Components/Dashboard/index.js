import React, { Fragment } from 'react'

import './index.css';

function Dashboard() {

    return (
        <Fragment>
            <div id="filters-main">
                <h2>Select your filters</h2>
            </div>
            <div id="db-main">
            </div>
        </Fragment>
    );
}

export default Dashboard;
