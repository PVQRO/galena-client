import React, { Fragment, useState } from 'react';

import './index.css'

function Filter(props) {

    /* 
        props must contain the title of the filter and 
        the options that can be set in the filter 
     */

    return (
        <Fragment>
            
            
        </Fragment>
    );
}

export default Filter;